# TRINETRA Frontend — Run Doc

## Reproduce uncommitted artifacts

No special env files needed. Dependencies are the only prerequisite:

```bash
cd Frontend
npm install
```

## Start the dev server

```bash
cd Frontend
npx vite --port 5173 --host
```

The Vite dev server serves at `http://localhost:5173/`. Hot-reload is automatic.
